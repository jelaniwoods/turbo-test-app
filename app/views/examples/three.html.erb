<h1>Examples#three</h1>
<p>Find me in app/views/examples/three.html.erb</p>

<turbo-frame id="custom-confirmation">
  <script>
    Turbo.config.forms.confirm = () => {
      let dialog = document.querySelector("dialog")
      dialog.showModal()
      return new Promise((resolve, reject) => {
        dialog.addEventListener("close", () => {
          resolve(dialog.returnValue == "confirm")
        }, { once: true })
      })
    }
  </script>
</turbo-frame>

<turbo-frame id="turbo-progress-green">
  <style>
    .turbo-progress-bar {
      height: 5px;
      background-color: green;
    }
  </style>

  <a href="/turbo/displaying_progress"
     data-turbo="true"
     data-turbo-stream="true">Reset</a>
</turbo-frame>

<turbo-frame id="turbo-progress-hidden">
  <style>
    .turbo-progress-bar {
      visibility: hidden;
    }
  </style>

  <a href="/turbo/displaying_progress"
     data-turbo="true"
     data-turbo-stream="true">Reset</a>
</turbo-frame>
